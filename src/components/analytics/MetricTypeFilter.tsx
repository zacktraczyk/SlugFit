import React from 'react';
import { MetricType } from '../../utils/analytics';

interface MetricTypeFilterProps {
  onChange: (m: MetricType) => void;
}

export const MetricTypeFilter: React.FC<MetricTypeFilterProps> = ({ onChange }) => {
  return null;
};
